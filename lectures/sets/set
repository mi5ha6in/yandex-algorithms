const LENGTH_SET = 10;
const set = new Array(10).fill([]);
const getHash = (value) => value % LENGTH_SET;

const checkValueInSet = (value) => {
  const hash = getHash(val)
  const listInHash = set[hash];
  const isValue = listInHash.fined((value, item) => value === item)
} 
const add = (item) => {
  const hash = getHash(item);
}